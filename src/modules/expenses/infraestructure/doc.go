// Package infraestructure holds the persistence adapters for the expenses BC.
// Postgres (cloud) and SQLite (sidecar) impls live under db/repositories with
// build tags — same posture as products / members / billing.
package infraestructure
